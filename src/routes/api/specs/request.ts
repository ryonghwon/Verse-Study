import { Router, Request, Response } from 'express'

const router = Router()

router.get('/request/list', (req: Request, res: Response) => {
  return
})

router.get('/request/new', (req: Request, res: Response) => {
  return
})

router.get('/request/:request_id', (req: Request, res: Response) => {
  return
})

router.delete('/request/:request_id', (req: Request, res: Response) => {
  return
})

export default router
